function values(name, lastName) {

// condition year the data is different to a text
if (typeof name !== "string" || typeof lastName !== "string") {
    return "value is not text";
  }

}

export { values };
